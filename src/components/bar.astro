---
const { ok, count, avgLatency, day } = Astro.props;

const ratio = ok/count;

const color = ratio > 0.98 ? 'bg-green-500/90' : ratio > 0.9 ? 'bg-yellow-500/90' : 'bg-red-500/90';

---

<div class={`${color} w-2 h-10 rounded-md`}/>
